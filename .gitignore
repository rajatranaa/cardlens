# Secrets — never commit
.env
.env.*
!.env.example
*token*.json
*credential*.json
client_secret*.json

# Local password builder — holds DOB/name/card details and derived passwords.
# Contains per-bank formulas + PII; MUST never be committed (security-guardrails).
tools/

# Statement PDFs must never be committed
*.pdf

# Java / Maven
backend/target/
*.class

# Node
mcp-server/node_modules/
node_modules/
npm-debug.log*

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store
